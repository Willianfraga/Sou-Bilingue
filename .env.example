# Sem banco por trás ainda (Fase 1 — ver docs/ESCOPO.md). O tutor de IA já
# está ligado de verdade em src/app/api/aula/chat/route.ts.

# Banco (Postgres, ex. Supabase — a decidir)
# DATABASE_URL=

# Tutor de IA — obrigatória para a tela /aluno/aula funcionar.
# Pegue a sua em https://console.anthropic.com/settings/keys e cole aqui
# (ou em .env.local, que nunca vai pro git).
ANTHROPIC_API_KEY=

# Fala (STT/TTS) — provedor a decidir
# SPEECH_API_KEY=
